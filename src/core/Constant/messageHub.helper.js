const _NOT_FOUND_MESSAGE ={
    message: 'Can not find',
    status: 404
};

const _INVALID_MESSAGE = {
    message: 'Invalid',
    status: 400
}

module.exports = { 
    _NOT_FOUND_MESSAGE,
    _INVALID_MESSAGE 
}